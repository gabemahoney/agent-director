---
id: t1.75s.tb
type: t1
title: 'TS/Bun client library (ts-bun-lib) — install resolution, smoke suite, envelope-diff, platform errors, catalog drift, publish guard'
parent: b.75s
children:
- t2.75s.tb.hg
- t2.75s.tb.yh
- t2.75s.tb.qo
- t2.75s.tb.ia
- t2.75s.tb.vf
- t2.75s.tb.4w
reference_materials: null
created_at: '2026-05-23T12:00:00+00:00'
status: pupa
schema_version: '0.1'
guid: 3a7f1c2d8e4b56091f0e2a3b4c5d6e7f
---

# TS/Bun client library (`ts-bun-lib`) testplan

t1 collector for the TypeScript/Bun client library (`pkg/ts-bun-client` — Epic 5
of `b.qe2`). Each t2 is a plain-shell case the Docker harness executes. Runs under
`make test-docker EPIC=ts-bun-lib`; the driver resolves the slug by
case-insensitive substring match against the t1 frontmatter title (see
`test/driver/run-testplan.sh`), so the canonical substring `ts-bun-lib` must
remain in the title verbatim.

## Sourced from

- Source Epic: `t1.qe2.ew` (TS/Bun client library, all verbs + publish pipeline).
- Testplan-adding Task: `t2.qe2.ew.ot`.

## Tests included

| t2 | what it pins |
|---|---|
| `t2.75s.tb.hg` — `ts-bun-1-install-resolves-only-host-platform` | `bun install` inside `pkg/ts-bun-client/` installs the `linux-x64` sub-package with its `.so`; darwin `.dylib` binaries are absent on the linux container. |
| `t2.75s.tb.yh` — `ts-bun-2-smoke-all-callable-verbs` | `bun test pkg/ts-bun-client/test/smoke/` exits 0 and at least 25 tests pass (every callable verb exercised). |
| `t2.75s.tb.qo` — `ts-bun-3-envelope-diff` | `make envelope-diff-ts` exits 0 — TS client output matches CLI subprocess per verb; no envelope diffs. |
| `t2.75s.tb.ia` — `ts-bun-4-unsupported-platform-init-error` | `_loadNativeInternal({ platform: 'linux', arch: 'mips' })` throws `ErrUnsupportedPlatform` whose message names the tuple `linux-mips`. |
| `t2.75s.tb.vf` — `ts-bun-5-error-subclass-drift` | Injecting `ErrFake` into `catalog.json` causes `errors-catalog-drift.test.ts` to fail with a message naming `ErrFake`. |
| `t2.75s.tb.4w` — `ts-bun-6-publish-blocked-by-placeholder` | `scripts/check-not-placeholder.ts` exits 1 with `CHANGEME-H3` in its output when run against the unmodified package. |

## Driver mode

All cases use `DRIVER_MODE=shell`. Each t2 body has a single fenced `bash`
block executed by the driver after the per-case DB-reset fixture.

All cases copy `/work/source` to a writable temp directory under `/tmp/`
and operate from there. `/work/source` itself is never written. Cases 2 and 3
rebuild `libagent_director.so` from source inside the container so the FFI
layer loads a container-native binary.

## Notes for the orchestrator

- Audit: `make test-docker EPIC=ts-bun-lib`; inspect the JSON stream case-by-case.
- The Dockerfile must have Bun installed (added to `test/Dockerfile` as part of
  this testplan — `ARG BUN_VERSION=1.3.13`, installed to `/opt/bun`, symlinked
  to `/usr/local/bin/bun`).
- Cases 2 and 3 require `CGO_ENABLED=1 go build -buildmode=c-shared` (gcc already
  in `test/Dockerfile` via `build-essential`).
- None of these cases use `agent-director spawn` or the harness `agent-director`
  binary at `/usr/local/bin/agent-director`. The DB-reset fixture's cd-session
  sweep is a no-op for all six cases.

## tmux session sweep

No `cd-*` sessions are created. The db-reset fixture's session sweep is a
no-op for these cases.
