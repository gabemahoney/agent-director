---
id: t1.75s.pr
type: t1
title: 'Epic 2 (b.3d3) — postinstall-platform-refusal testplan'
parent: b.75s
children:
- t2.75s.pr.b6
reference_materials: null
created_at: '2026-05-24T00:00:00+00:00'
status: pupa
schema_version: '0.1'
guid: 75sprb6vqsdh4n9p3kgwm2y6t8jqcurz
---

# Epic 2 (`b.3d3`) — `postinstall-platform-refusal` testplan

t1 collector for Epic 2 of Plan Bee `b.3d3` (umbrella + postinstall
platform refusal — `t1.3d3.n3`). Runs under
`make test-docker EPIC=postinstall-platform-refusal`; the driver
resolves the slug by case-insensitive substring match against this t1
title (`test/driver/run-testplan.sh`), so `postinstall-platform-refusal`
must remain in the title verbatim.

## Sourced from

- Plan Bee: `b.3d3`.
- Epic ticket: `t1.3d3.n3` (Acceptance Criteria around the postinstall
  host-pair refusal for darwin/x64 + linux/arm64).
- SRD: `t1.fg3.7i` §SR-3.2 (host-pair refusal at the top of
  postinstall.ts), §SR-6.3 (per-Epic Docker testplans).

## Tests included

| t2 | what it pins |
|---|---|
| `t2.75s.pr.b6` — `bootstrap-6-host-pair-refused` | Spawn `bun run scripts/postinstall.ts` directly with `Object.defineProperty`-spoofed `process.platform`/`process.arch` for both `darwin/x64` and `linux/arm64`. Each sub-invocation exits non-zero with the SR-3.2 message before any filesystem write under `${HOME}/.claude/skills/`. |

## Driver mode

`DRIVER_MODE=shell`. The t2 body carries a single fenced ` ```bash `
block executed by the driver after the per-case DB-reset fixture.

## Coverage outside this testplan

- The Windows AC 3 leg (coarse `os` gate at npm/bun resolution time)
  is operator-verified outside CI per SRD SR-6.3 (no Windows harness in
  CI).
- Genuine darwin/x64 and linux/arm64 hardware legs are operator-verified
  outside CI; this testplan simulates them via env-spoofing on the
  linux/x86_64 harness.

## Notes for the orchestrator

- Audit: run `make test-docker EPIC=postinstall-platform-refusal` and
  inspect the JSON stream.
- The DB-reset fixture is a no-op for these cases — no
  `~/.agent-director/state.db` interactions occur.
- The case is hermetic — no fixture cache, no `bun pm pack`. It spawns
  the in-tree `postinstall.ts` directly from the read-only `/work/source`
  mount, exercising the gate without any tarball or consumer install.

## tmux session sweep

The case does NOT exercise `agent-director spawn`. No `cd-*` sessions
are created; the DB-reset fixture's sweep is a no-op for this case.
