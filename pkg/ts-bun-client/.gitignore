dist/
node_modules/
*.tsbuildinfo

# Native binaries dropped by CI / prepare-platforms — never committed.
platforms/*/libagent_director.so
platforms/*/libagent_director.dylib
# CLI binary staged by test/setup.ts for local resolveCliPath() resolution.
platforms/*/bin/

# Temp dirs created by install-resolution test
test/.tmp-*

# Skill body staged at pack time by scripts/stage-skill.ts (the prepack
# hook). Source of truth lives at ${REPO_ROOT}/skills/install-agent-director/;
# the staged copy is purely a packaging artifact and must never be committed.
skills/
